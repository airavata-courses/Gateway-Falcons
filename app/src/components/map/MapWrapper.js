import React from 'react';

const MapWrapper = () => {
    return (
        <div style={{
            position: 'relative',
            width: "100%",
            height: "100%"
        }} />
    );
}

export default MapWrapper;